import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // Cuando uses un dominio propio (www.ejemplo.com), borra o comenta la siguiente línea.
  base: '/boda-abrahan-tete/',
})
